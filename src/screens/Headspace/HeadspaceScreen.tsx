import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { Headspace } from "./Headspace";

const HeadspaceScreen = () => {
  return <Headspace />;
};

export default HeadspaceScreen;

const styles = StyleSheet.create({});
